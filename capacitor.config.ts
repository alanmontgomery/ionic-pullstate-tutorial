import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-pullstate-tutorial',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
